spring:
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties.hibernate.format_sql: true
  batch:
    job:
      enabled: false


mybatis:
  configuration:
    map-underscore-to-camel-case: true
  mapper-locations: mapper/*.xml
